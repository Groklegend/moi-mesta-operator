-- ============================================================
-- Миграция 28: уточнение по адресу встречи
-- ============================================================
-- Оператор в «Заявка Плюс» под полем «Адрес встречи» теперь может
-- добавить отдельную пометку (например, «встреча в кафе» или название
-- БЦ). Чтобы не смешивать с самим адресом, кладём в новый столбец.
-- Менеджер видит это поле в карточке лида и в деталях события календаря.

alter table public.leads
  add column if not exists meeting_address_note text;
